#include <stdlib.h>
#include <string.h>
#include <stdint.h>

#include "helpers.h"
#include "lib_bfs.h"

#define MAX_LINE 256

int load_file(llist_t* db, FILE* fp)
{
    if (!db || !fp) { return 0; }

    return 1;
}